name: SEAndroid v2.0.1 Final Build
run-name: SEAndroid v2.0.1 Final Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Final Build Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Validate workflow
        run: |
          echo "FINAL_WORKFLOW_OK"
          test -f SEAndroid_v100.0.1.zip
          test -f patch_parts/00.b64
