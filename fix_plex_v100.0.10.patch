--- a/app/src/main/java/com/orbital/iptv/data/plex/PlexRepository.kt
+++ b/app/src/main/java/com/orbital/iptv/data/plex/PlexRepository.kt
@@ -334,9 +334,6 @@
         val req = serverBuilder(serverUrl, "/library/metadata/$ratingKey/children", token).build()
         parseMetadata(client.newCall(req).execute().use { it.body?.string() ?: "" })
     }
-            }
-        }
-    }
 
     // ── Playback reporting ────────────────────────────────────────────────────
 
--- a/app/src/main/java/com/orbital/iptv/ui/series/SeriesActivity.kt
+++ b/app/src/main/java/com/orbital/iptv/ui/series/SeriesActivity.kt
@@ -113,6 +113,147 @@
         }
     }
 
+    private fun buildCategoryMenu(categories: List<SeriesCategory>, selected: SeriesCategory?) {
+        val container = binding.catContainer
+        container.removeAllViews()
+
+        val p = ThemeManager.palette()
+        val density = resources.displayMetrics.density
+        val rowH = (46 * density).toInt()
+        val pad = (16 * density).toInt()
+        val marginPx = (p.itemMarginDp * density).toInt()
+
+        val allFavs = FavouritesManager.getAll(this)
+        val favItems = allFavs.filter { it.type == FavType.SERIES || (it.type == FavType.EPISODE && !it.hasResume && !it.isUpNext && it.seriesId >= 0) }
+        val continueItems = allFavs.filter { it.type == FavType.EPISODE && (it.hasResume || it.isUpNext) }
+
+        val recent = TextView(this).apply {
+            val lp = LinearLayout.LayoutParams(LinearLayout.LayoutParams.MATCH_PARENT, rowH)
+            if (marginPx > 0) lp.setMargins(marginPx, marginPx / 2, marginPx, marginPx / 2)
+            layoutParams = lp
+            gravity = Gravity.CENTER_VERTICAL
+            setPadding(pad, 0, 0, 0)
+            text = "RECENTLY ADDED"
+            textSize = 13f
+            typeface = Typeface.create("sans-serif-condensed", Typeface.NORMAL)
+            isClickable = true
+            isFocusable = true
+            background = ThemeManager.roundedBg(if (showingRecentlyAdded) p.highlight else p.bgMid, density)
+            setTextColor(if (showingRecentlyAdded) 0xFF000000.toInt() else 0xFFFFFFFF.toInt())
+            setOnFocusChangeListener { _, hasFocus ->
+                if (!showingRecentlyAdded) background = ThemeManager.focusRowDrawable(density, p.bgMid, hasFocus)
+            }
+            setOnClickListener {
+                showingRecentlyAdded = true
+                showingFavourites = false
+                showingContinue = false
+                val sorted = allShows.sortedByDescending { recentSortKey(it.added ?: it.lastModified) }
+                adapter.submitList(sorted)
+                binding.tvShowCount.text = "${sorted.size} SHOWS"
+                buildCategoryMenu(categories, selected)
+            }
+        }
+        container.addView(recent)
+
+        if (favItems.isNotEmpty()) {
+            val isSel = showingFavourites
+            container.addView(TextView(this).apply {
+                val lp = LinearLayout.LayoutParams(LinearLayout.LayoutParams.MATCH_PARENT, rowH)
+                if (marginPx > 0) lp.setMargins(marginPx, marginPx / 2, marginPx, marginPx / 2)
+                layoutParams = lp
+                gravity = Gravity.CENTER_VERTICAL
+                setPadding(pad, 0, 0, 0)
+                text = "★ FAVOURITES"
+                textSize = 13f
+                typeface = Typeface.create("sans-serif-condensed", Typeface.NORMAL)
+                isClickable = true
+                isFocusable = true
+                background = ThemeManager.roundedBg(if (isSel) p.highlight else p.bgMid, density)
+                setTextColor(if (isSel) 0xFF000000.toInt() else 0xFFFFFFFF.toInt())
+                setOnFocusChangeListener { _, hasFocus ->
+                    if (!isSel) background = ThemeManager.focusRowDrawable(density, p.bgMid, hasFocus)
+                }
+                setOnClickListener {
+                    showingFavourites = true
+                    showingContinue = false
+                    showingRecentlyAdded = false
+                    showFavouritesSeries()
+                    buildCategoryMenu(categories, selected)
+                }
+            })
+        }
+
+        if (continueItems.isNotEmpty()) {
+            val isSel = showingContinue
+            container.addView(TextView(this).apply {
+                val lp = LinearLayout.LayoutParams(LinearLayout.LayoutParams.MATCH_PARENT, rowH)
+                if (marginPx > 0) lp.setMargins(marginPx, marginPx / 2, marginPx, marginPx / 2)
+                layoutParams = lp
+                gravity = Gravity.CENTER_VERTICAL
+                setPadding(pad, 0, 0, 0)
+                text = "▶ CONTINUE WATCHING"
+                textSize = 13f
+                typeface = Typeface.create("sans-serif-condensed", Typeface.NORMAL)
+                isClickable = true
+                isFocusable = true
+                background = ThemeManager.roundedBg(if (isSel) p.highlight else p.bgMid, density)
+                setTextColor(if (isSel) 0xFF000000.toInt() else 0xFFFFFFFF.toInt())
+                setOnFocusChangeListener { _, hasFocus ->
+                    if (!isSel) background = ThemeManager.focusRowDrawable(density, p.bgMid, hasFocus)
+                }
+                setOnClickListener {
+                    showingContinue = true
+                    showingFavourites = false
+                    showingRecentlyAdded = false
+                    showContinueWatching()
+                    buildCategoryMenu(categories, selected)
+                }
+            })
+        }
+
+        val offset = 1 + (if (favItems.isNotEmpty()) 1 else 0) + (if (continueItems.isNotEmpty()) 1 else 0)
+        val visibleCategories = categories.filter { !it.categoryName.equals("SERIES", true) }
+        visibleCategories.forEachIndexed { i, cat ->
+            val isSelected = !showingContinue && !showingFavourites && !showingRecentlyAdded && cat.categoryId == selected?.categoryId
+            val idx = i + offset
+            val normalBg = if (idx % 2 == 0) p.bgMid else p.bgPrimary
+            container.addView(TextView(this).apply {
+                val lp = LinearLayout.LayoutParams(LinearLayout.LayoutParams.MATCH_PARENT, rowH)
+                if (marginPx > 0) lp.setMargins(marginPx, marginPx / 2, marginPx, marginPx / 2)
+                layoutParams = lp
+                gravity = Gravity.START or Gravity.CENTER_VERTICAL
+                setPadding(pad, 0, 0, 0)
+                val profile = PrefsManager.getActiveProfile(this@SeriesActivity)
+                val favNow = profile?.let { FavouritesManager.containsCategory(this@SeriesActivity, "SERIES", it.serverUrl, cat.categoryId) } == true
+                text = cat.categoryName + if (favNow) "   ♥" else "   ♡"
+                textSize = 13f
+                typeface = Typeface.create("sans-serif-condensed", Typeface.NORMAL)
+                isClickable = true
+                isFocusable = true
+                background = if (isSelected) ThemeManager.roundedBg(p.highlight, density) else ThemeManager.roundedBg(normalBg, density)
+                setTextColor(if (isSelected) 0xFF000000.toInt() else 0xFFFFFFFF.toInt())
+                setOnFocusChangeListener { _, hasFocus ->
+                    if (!isSelected) background = ThemeManager.focusRowDrawable(density, normalBg, hasFocus)
+                }
+                setOnTouchListener { v, e ->
+                    if (e.action == MotionEvent.ACTION_UP && e.x > v.width - (56 * density)) {
+                        val profile = PrefsManager.getActiveProfile(this@SeriesActivity)
+                        if (profile != null) {
+                            (v as TextView).text = cat.categoryName + if (FavouritesManager.toggleCategory(this@SeriesActivity, "SERIES", cat.categoryId, cat.categoryName, profile.serverUrl, profile.id)) "   ♥" else "   ♡"
+                        }
+                        true
+                    } else false
+                }
+                setOnClickListener {
+                    showingContinue = false
+                    showingFavourites = false
+                    showingRecentlyAdded = false
+                    viewModel.selectCategory(cat)
+                }
+            })
+        }
+    }
+
     private fun recentSortKey(raw: String?): Long {
         val v = raw?.trim().orEmpty()
         v.toLongOrNull()?.let { return it }
--- a/app/src/main/java/com/orbital/iptv/recording/RecordingsActivity.kt
+++ b/app/src/main/java/com/orbital/iptv/recording/RecordingsActivity.kt
@@ -27,6 +27,9 @@
 import kotlinx.coroutines.flow.collectLatest
 import kotlinx.coroutines.launch
 import java.io.File
+import java.text.SimpleDateFormat
+import java.util.Date
+import java.util.Locale
 
 class RecordingsActivity : AppCompatActivity {
